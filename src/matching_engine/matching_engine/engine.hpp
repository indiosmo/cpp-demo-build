#ifndef MATCHING_ENGINE_ENGINE_HPP
#define MATCHING_ENGINE_ENGINE_HPP

#include "matching_engine/v3/engine.hpp"

/* Production matching-engine alias; v1/v2/v3 headers expose the historical iterations. */

namespace matching_engine {

using engine = v3::engine;

} // namespace matching_engine

#endif /* MATCHING_ENGINE_ENGINE_HPP */
